import styled from '@emotion/styled';

const FavouriteCatalogueWrapper = styled.div`
  .favourite-catalogue-section-wrapper {
    display: flex;
    flex-wrap: wrap;
  }
`;

export default FavouriteCatalogueWrapper;
